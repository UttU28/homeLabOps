# Homelab edge nginx — proxy habit.thatinsaneguy.com to in-cluster Ingress.
# TLS is issued by cert-manager and terminated inside the cluster (ingress-nginx :30443).
#
# Install: sudo homeLabOps/scripts/configureEdgeNginx.sh
# Requires: platform ingress-nginx synced (NodePorts 30080 / 30443)

server {
    listen 80;
    listen [::]:80;
    server_name habit.thatinsaneguy.com;

    location / {
        proxy_pass http://127.0.0.1:30080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name habit.thatinsaneguy.com;

    location / {
        proxy_pass https://127.0.0.1:30443;
        proxy_ssl_verify off;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    ssl_certificate /etc/letsencrypt/live/habit.thatinsaneguy.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/habit.thatinsaneguy.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}
