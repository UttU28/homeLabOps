apiVersion: apps/v1
kind: Deployment
metadata:
  name: saral-ui
  labels:
    app.kubernetes.io/name: saral-ui
    app.kubernetes.io/component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: saral-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: saral-ui
        app.kubernetes.io/component: frontend
    spec:
      containers:
        - name: ui
          image: ghcr.io/uttu28/saral-ui:latest
          ports:
            - name: http
              containerPort: 9261
          readinessProbe:
            httpGet:
              path: /
              port: 9261
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 9261
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
