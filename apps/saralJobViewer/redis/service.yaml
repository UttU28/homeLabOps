apiVersion: v1
kind: Service
metadata:
  name: saral-redis
  labels:
    app.kubernetes.io/name: saral-redis
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saral-redis
  ports:
    - name: redis
      port: 9262
      targetPort: 9262
